# 実装計画

> 2026-09-13 に design-doc 8 章の制御モデル（coordinator があるべき状態を作り、proxy がそれを実現する）に合わせて改訂した。旧方式（proxy が予算をリースし、coordinator は帳簿に徹する）の記述は残していない。

M1 で単体の予算スケジューラを作り、M2 でそれを分散させる。**M1 の完了時点で、既存プーラーが解いていない 3 問題のうち 2 つが解けている状態**にする。各ステップは 1 つの小さな CL に対応し、テストを先に書く。

本書では契約も決定も**名前で呼ぶ**。読むときに記号を引き直さなくて済むようにするためである。

## 進捗

| ステップ | 状態 | 成果物 |
|---|---|---|
| M1-01 足場、時間と入出力の抽象化、テストハーネス | 完了（2026-09-13、PR #1） | workspace と CI、`pgsteward-core::rt`（`Clock` / `Spawner` / `Net`、tokio 実装と turmoil 実装）、`pgsteward-node::config`（2 層の TOML、総予算の項目なし）、`tests/harness`（偽 PostgreSQL、`pg_stat_activity` 監視、`CapMonitor`） |
| M1-02 フレーミングと ReadyForQuery 状態機械 | 完了（2026-09-13、PR #1） | `pgsteward-protocol::{framing, message, ready}` |
| M1-03 スタートアップの判別 | 完了（2026-09-13、PR #2） | `pgsteward-protocol::startup`（SSLRequest / GSSENCRequest / CancelRequest / StartupMessage の判別と符号化）、`pgsteward-core::tenant`（`TenantId`） |
| M1-04 DB 接続の確立と総予算の導出 | 完了（2026-09-14、PR #7）。1 つの CL には大きいので、総予算の導出（純粋関数、PR #3）→ DB 接続の確立（trust と MD5、PR #4）→ SCRAM 認証（PR #5）→ `SHOW` と `pg_stat_activity` の読み取り（PR #6）→ 他者の接続の観測ピークと導出値の更新（PR #7）、の順に分けて進めた | `pgsteward-core::budget`（`ServerLimits`、`BudgetInputs`、`TotalBudget`、`ForeignPeak`、`InstanceBudget`、`BudgetChange`）、`pgsteward-core::server`（`ApplicationName`、`ServerCredentials`、`ServerConnection::handshake`（trust / MD5 / SCRAM-SHA-256）、`connect`、`SimpleQuery`）、`pgsteward-core::inspect`（`read_server_limits`、`read_timeout_settings`、`count_foreign_connections`） |
| M1-05 クライアント認証 | 完了（2026-09-15、PR #10）。1 つの CL には大きいので、スタートアップの応答とテナントの解決（暗号化要求の拒否と trust 相当の AuthenticationOk、PR #8）→ SCRAM-SHA-256 でのクライアント認証（PR #9）→ 資格情報の読み込み（PR #10）、の順に分けて進めた。**サーバー側の SCRAM は自前で書いた** — `scram-rs` はサーバー側の検証に SaltedPassword を要求し、PostgreSQL が実際に持つ verifier（salt・反復回数・StoredKey・ServerKey）だけでは検証できない。部品は `hmac` / `sha2` / `base64` / `stringprep`（SaslPrep）/ `subtle` で揃い、PBKDF2 は反復そのものなので依存を増やさず書いた。**資格情報のないユーザーにも交換を最後まで演じる** — PostgreSQL の mock authentication と同じく、使い捨ての verifier で必ず証明に失敗させ、ユーザー名の存在を問い合わせで判別できないようにする。**クライアントの資格情報はノードローカル設定から読む** — `[client."…"]` に、PostgreSQL が `pg_authid.rolpassword` に持つのと同じ形式の verifier を書く。平文のパスワードは項目そのものがないので受け付けない。認証はユーザー単位なので表もユーザー名で引く。`auth_query` 相当（DB に問い合わせて verifier を引き、キャッシュする）は同じ `Credentials` の別実装として後から足せる。DB への認証情報と同じくノードローカルに置くのは、クラスタ設定が Raft ログとして全 coordinator のディスクに残るためである | `pgsteward-protocol::backend`（`EncryptionResponse`、`ErrorResponse`、`encode_authentication_ok`、`encode_authentication_sasl` とその継続・終了）、`pgsteward-protocol::frontend`（`decode_sasl_initial_response`）、`pgsteward-core::scram`（`ScramVerifier`、`ScramExchange`、`ScramError`、`VerifierError` と verifier の読み取り）、`pgsteward-core::auth`（`AuthMethod`、`Credentials`、`TrustAll`、`ClientCredentials`）、`pgsteward-core::session`（`accept`、`ClientSession`、`Accepted`、`AcceptError`）、`pgsteward-node::config`（`ClientSection`、`client_credentials`） |
| M1-06 セッションモードの透過プロキシ | 完了（2026-09-17、PR #11） | 認証を終えたクライアントに、DB 側の ParameterStatus をそのまま、BackendKeyData、ReadyForQuery（アイドル）の順で返してから、双方向のバイト列のコピーに入る。**バイト列は解釈せずに流す** — セッションモードではメッセージ境界を見る必要がないので、フレームの解析を入れない。extended query も COPY もそのまま通る。境界を見始めるのは M1-08 から。**握手の途中で読んでしまったバイト列を先に流す** — クライアントが startup に続けて送ってきた分と、DB 側の読み残しの両方。**キャンセル鍵は DB のものをそのまま渡す** — 自前で生成するのは M1-14。**クライアントの startup パラメータは DB 接続に引き継がない** — DB 接続はノードローカルの資格情報で張るためで、`options` や `search_path` の扱いは M1-12 の状態リセットと合わせて決める。片側が閉じたらもう片側の書き込みを閉じ、両方向が終わるまで待つ。シミュレーションには入れていない — 偽 PostgreSQL が startup に応答しないためで、M1-07 のプールで必要になったところで拡張する | `pgsteward-protocol::backend`（`encode_parameter_status`、`encode_backend_key_data`、`encode_ready_for_query`）、`pgsteward-protocol::message`（`TransactionStatus` からバイトへの変換）、`pgsteward-core::relay`（`session_mode`、`RelayError`）、`pgsteward-core::server`（`ServerConnection::into_parts`）、統合テスト `session_mode`（実 PostgreSQL 16 に対する SELECT、一時テーブルで確かめる 1 対 1、エラーの透過） |
| M1-07 プールと待機キュー | 完了（2026-09-17、PR #14）。1 つの CL には大きいので、プールと FIFO 待機キュー（PR #12）→ ノードのクライアント接続の受け入れ上限（PR #13）→ 偽 PostgreSQL の startup 応答とシミュレーションでの検証（PR #14）、の順に分けて進めた。**上限は固定値で、割当表の払い出しから取るように差し替えるのは M1-10。** **接続は遅延生成するが、先に枠を予約してから開く** — 開いている途中も予約が枠を占めるので、同時に開いても実接続が上限を超えない。開くのに失敗したら予約は解放され、待機者がいればその枠をそのまま渡す。**解放された接続は待機キューの先頭へ直接手渡す** — いったんアイドルに戻して取り合わせると、後から来たクライアントに横取りされて FIFO が崩れる。手渡しは割り当てごとの番号札で追跡し、待機タイムアウトで抜けたクライアントは自分の札をキューから外す。手渡しと入れ違いになった場合は受け取った枠を使う。**アイドルタイムアウトは持たない**（design-doc 8.4）。**待機タイムアウトはノードローカル設定の項目にしていない** — design-doc 14.1 に項目がないので、今は呼び出し側が渡す。時計は `rt::Clock` 越しにしか触らないので、待機と満了は擬似時間で試験できる。**クライアント接続を受け入れるかはノードが単独で決める**（design-doc 8.2）— 受け入れ上限はノードローカル設定の `max_client_connections` で、上限に達していたら、**クライアントが送ってきたパケットを読み切ってから** SQLSTATE 53300 の FATAL を書き、write 側を閉じる。断る相手を認証せず、テナントも解決せず、DB 接続も張らないことは変わらない。**読まずに閉じてはいけない** — 未読のデータを残したまま閉じるとリセットになり（RFC 9293 3.10.4）、断った理由ごと捨てられる。シミュレーションはこれを忠実に模しており、読まない実装は乱数シードの 4 割で落ちた。**席は接続枠ではない** — クライアント接続 1 本は払い出しを消費しないので、この数は割当表と関係しない。席は `Admitted` が持ち、落としたときに返る。同時に到着したクライアントが上限を追い越さないよう、席の取得は compare-and-swap で行う。**偽 PostgreSQL が startup に応答するようにした** — trust 相当で AuthenticationOk、ParameterStatus、BackendKeyData、ReadyForQuery（アイドル）の順に返し、simple query には接続ごとの識別子を 1 行だけ返す。実 PostgreSQL の代わりではなく、**どのクライアントがどの DB 接続で走ったかを数えられる最小限**で、`SELECT pg_backend_pid()` に相当する役目だけを持つ。StartupMessage 以外の startup 要求には応答せず閉じる。**プールの検証をシミュレーションに移せた** — 実 DB も Docker も要らず、待機タイムアウトは擬似時間で満了するので、統合テストのように実時間を待たない | `pgsteward-core::pool`（`OpenServer`、`InstanceOpener`、`Pool`、`PoolLimits`、`PoolStats`、`Assigned`、`PoolError`）、統合テスト `pool`（実 PostgreSQL 16 に対して、クライアント 8 件を枠 2 つで捌き、`CapMonitor` で上限の超過が一度もないことを確認する）、`pgsteward-core::admission`（`ClientLimit`、`Admitted`、`ClientLimit::accept`）、`pgsteward-core::session`（`AcceptError::TooManyClients`）、`pgsteward-protocol::backend::sqlstate::TOO_MANY_CONNECTIONS`、`pgsteward-node::config`（`NodeConfig::client_limit`）、`pgsteward-harness::fake_postgres`（startup への応答、simple query への 1 行の応答）、シミュレーションテスト `fake_postgres`（握手と接続ごとの識別子）と `pool`（枠 2 つでクライアント 8 件を捌き、`CapMonitor` で上限の超過が一度もないこと、待機タイムアウトで諦めたクライアントが DB 接続を開かないこと） |
| M1-08 トランザクションモード | 完了（2026-09-17）。**境界は `ReadyTracker::may_release()` が決める** — ReadyForQuery がアイドルを示し、未処理の要求がなく、extended query の窓が開いていないときだけ返す。トランザクション中と失敗したトランザクション中は保持するので、途中で別のクライアントに渡らない。**ここからフレーム単位で中継する** — セッションモード（M1-06）はバイト列をそのまま流していたが、境界を見るには区切りが要る。**パイプラインされた要求は届いた順に転送する** — 転送のたびに未処理の数を数えるので、先行する要求の ReadyForQuery では返さない。**Terminate はサーバーに渡さない** — プールに戻る DB 接続を閉じてしまうため。**最初のメッセージが来るまで割り当てない** — クライアントが黙っている間は枠を占めない。**挨拶は最初のメッセージを待てない** — クライアントは ReadyForQuery を待ってから送るので、最初のクライアントだけが DB 接続を 1 本借りて ParameterStatus を写し取り、すぐ返す。以後のクライアントはプールに触らずに挨拶される。**キャンセル鍵は自前で作る** — DB のものを渡すと、その接続を次に使うクライアントのクエリを止められてしまう。転送とルーティングは M1-14。**待機タイムアウトに達したら SQLSTATE 53300 の FATAL を書いて閉じる** — 黙って切ると理由が残らない。**枠を手放す前に、接続が DB から消えたことを確かめる** — 汚れた接続を捨てるときは Terminate を送って EOF まで読み、それから枠を空ける。ソケットを落とすだけでは DB はまだその接続を数えており、置き換えの接続が先に開くと実接続が総予算を超える。シミュレーションでシードを振ったところ、40 本のうち数本で実際に上限 1 に対して実接続 2 を観測した。最優先の不変条件なので `CloseServer` を足して閉じ切る順序にし、テストはシードを 40 本振って確かめる。**トランザクションの途中でクライアントが去った接続は捨てて張り直す** — 開いたままのトランザクションを次のクライアントに渡さない。`DISCARD ALL` による通常の返却時のリセットは M1-12。**extended query は素通しで動くが契約は M1-11** — `ReadyTracker` が Sync を数えるので境界は正しく出るものの、Flush の扱いを含めて確かめるのは M1-11。**偽 PostgreSQL に BEGIN / COMMIT のトランザクション状態を持たせた** — 境界の保持をシミュレーションで見るための最小限で、SQL を解釈するのは先頭の 1 語だけ | `pgsteward-core::relay`（`serve_assignment`、`Boundary`、`transaction_mode`、`Welcome`、`RelayError`）、`pgsteward-core::pool`（`CloseServer`、`Assigned::discard`）、`pgsteward-core::server`（`ServerConnection::read_frame`、`forward`、`close`）、`pgsteward-protocol::framing::MAX_MESSAGE`、`pgsteward-harness::fake_postgres`（トランザクション状態）、テスト `relay`（境界 9 本）、`pool`（捨てた接続の枠）、シミュレーション `transaction_mode`（1 枠を 2 クライアントで共有、開いたトランザクションによる保持、途中離脱した接続の破棄をシード 40 本で、待機タイムアウトの通知）、統合テスト `transaction_mode`（実 PostgreSQL 16 と tokio_postgres 8 クライアントを枠 2 つで捌き、トランザクション中は同じバックエンド、`CapMonitor` で超過なし） |
| M1-09 配分アルゴリズム | 完了（2026-09-18）。**配分器は純粋関数で、入出力の型が契約である** — 総予算と、テナント×proxy ごとの最低保証・上限・重み・需要を受け取り、あるべき状態（目標の払い出し量）を返す。入出力に時計も入出力も現れないので、M2 で入力が 1 ノード分から全ノード分に変わってもテストはそのまま効く。**最低保証を配ってから余剰を配る、2 段階の water-filling**。どちらの段階も 1 本ずつ配る貪欲法で、次の 1 本は「その段階で取った本数 ÷ 重み」が最小のものへ渡す。**1 本ずつ配るので、総予算を 1 増やしたときの配分は、増やす前の配分にもう 1 本足したものになる** — 単調性が手続きから従い、証明のために別の仕掛けを持たない。**整数丸めの誤差は繰り越す** — 比は交差乗算で比べ、割り算の切り捨てをどこにも残さない。**余剰の取り分は重みだけで決まり、最低保証は勘定に入れない**（design-doc 14.1 の `weight` は「余剰の取り分の重み」）。**需要が 0 なら最低保証があっても 0 本**（design-doc 14.1）— 最低保証は「需要があるときに必ず取れる下限」であって、確保しておく本数ではない。**総予算が最低保証の合計を下回ったときの縮小は、同じ手続きから出る** — 最低保証を配る段階そのものが重み付き max-min fair なので、予算が尽きれば重みに比例した配分で止まる。黙って縮めないために、足りなかった本数を `unmet_guarantee` として出力に載せる。**重みは `NonZeroU32`** — 0 は比の比較が定義できず、既定値も 1 なので、型で表現できないようにした。**入力の順序に依存しない** — キー順に整列してから配り、同点はキーで割るので、走査順という実装の詳細が配分に影響しない。**ヒステリシスは 3 段階目として入れた** — 最低保証・需要を配ったあとに残った予算の範囲でだけ、各テナントが今持っている本数を保たせる。**需要のあるテナントより後に配るので、保持が誰かを飢えさせることはない**。従来のプーラーが `server_idle_timeout` で持っていた判断はここに吸収される（design-doc 8.4）。**保持は 1 調停ごとに `release` 本ずつ減る** — 減らさないと、需要が消えたテナントが予算の空いている限り枠を持ち続け、最低保証 0 のテナントが需要のない間に資源を持たないという性質（M1-10）が成り立たなくなる。`release` の既定値は 1 本だが、**これは暫定値である** — 速すぎれば接続チャーン、遅すぎれば遊休という交換で、既定値は実測で決めるほかない（design-doc 19 章の「枠の移動の最小間隔」）。**同点なら今持っているテナントに渡す** — 重みが同じなら同点は頻繁に起きるので、比が等しいときは新しく張る側より持っている側を先にして、動かさずに済む枠を動かさない | `pgsteward-sched`（`Claim`、`Grants`、`Allocator`、`fair::WeightedMaxMinFair`）、プロパティテスト（総和・最低保証・上限・決定性・単調性の 5 性質。決定性は入力を並べ替えても同じ配分になること、単調性は総予算を増やしたときと 1 テナントの需要を増やしたときの両方で確かめる）、テナント 30,000 件を総予算 100 で配る場合と、3 テナントが競合しても飢餓が起きない場合、ヒステリシスの性質（保持できる上限は「需要と、持っている本数から `release` を引いたもの」の大きいほう、需要が保持に優先すること、手放しが 1 調停ごとに進んで 0 に落ち着くこと、何も変わらない調停では枠が動かないこと） |
| M1-10 割当表・配分器・制御ループの縮退形 | 着手（2026-09-18）。1 つの CL には大きいので、プールの収束（PR #17）→ 割当表と事前条件検査（PR #18）→ `GrantChannel` と `InProcessCoordinator`（PR #19）→ proxy 側の需要の報告と払い出しへの収束（本 CL）→ テナント設定の対応づけと需要の消えたプールの破棄、の順に分けて進める。`GrantChannel` を入れた時点で、coordinator 側と proxy 側を別の CL に分けた — 両方を 1 つにすると 300 行を大きく超える。**割当表は「インスタンス × テナント × proxy → 接続枠の本数」で、書き込みは 1 エントリで丸ごと入れ替える**（design-doc 8.3 の③）— エントリに出てこない保有者は 0 本になる。差分を書き足す形にすると、エントリが失われたときに表が過去の払い出しを持ち続ける。**事前条件は「インスタンスごとに、払い出しの総和 ≤ 総予算」の 1 つだけ**（design-doc 8.4 の 1 番目の性質、`plan.md` 4.1 の規則 1）。破るエントリは**そのエントリ全体を拒む** — 一部だけ入れると、拒まれたインスタンスだけ古い払い出しが残った表になる。**総予算も割当表に載せ、エントリで払い出しと一緒に動かす** — 総予算は他者の接続の観測で縮むので（M1-04）、縮小だけを先に書けるようにすると、事前条件を満たさない表を作れてしまう。縮めるときは、縮んだ予算に収まる払い出しを同じエントリに入れる。**払い出し 0 本は表に載せない** — 需要のないテナントを 0 本の行として持たない（M1-10 のゼロ保証テナントの性質）。**期限とフェンシングトークンはまだ載せていない** — 縮退形では払い出しの相手が自分自身しかいないので、失効は M2-03 で入れる。**proxy が見るのは `GrantChannel` だけ** — 払い出しを受け取る口（`watch` で最新の払い出しを配る）と、需要と実接続を報告する口の 2 つで、払い出しがどこで計算されたかを知らない（1.1 節）。**`InProcessCoordinator` は調停 1 回で、インスタンスごとに配分器を回し、割当表に 1 エントリで書き、払い出しを配る**。払い出しが変わったときだけ世代を進める。**回収した枠は、回収の完了が報告されるまで他へ払い出さない**（`plan.md` 4.1 の規則 5）。縮退形でも同じ問題がある — 1 つのプロセスの中でも、テナント A のプールが閉じ終える前にテナント B のプールが開けば、実接続が総予算を超える。そのため保有者ごとに「まだ DB にありうる本数」を持つ。増えた払い出しは即座にその本数に数え（proxy はいつでもそこまで開けるため）、減った払い出しは、**減らしたあとの世代に対する報告**が実接続の減少を示すまで元の本数のまま数える。増やす払い出しは、総予算からこの本数の合計を引いた残りの範囲でだけ出す。**報告には、どの世代の払い出しに対して測ったかを載せる** — 縮小より前の世代の報告は、縮小を見ていない proxy の値なので解放に使わない。M2 ではこの世代が Raft のログインデックス（フェンシングトークン）に置き換わる。**総予算の縮小も同じ規則で扱う** — 縮んだ予算の範囲で配分し、超過が閉じ終わるまで増やす側を待たせる（design-doc 14.1「総予算を変更するとき」）。残りが足りないときに増やす側のどれへ先に渡すかはキー順で、次の調停で配分器の目標に近づく。**報告は全量で送る** — 載っていないテナントは需要も実接続も 0 と見なす。差分にすると、失われた報告が需要を残し続ける。**proxy 側は `ProxyPools` が持つ** — インスタンス×テナントごとのプールで、**枠数の初期値は 0 本**。払い出しを受けるまで 1 本も開かない。制御ループは、払い出しが変わったときと一定間隔ごとに、全プールを最新の払い出しへ収束させ（払い出しに載っていないプールは 0 本へ）、**収束させてから**報告する。報告がその世代の払い出しに対して測ったものになるようにするためである。**需要は待機中のクライアントと使用中の接続の合計**（design-doc 9 章）で、開いている途中の接続も使用中に数える — その接続を待っているクライアントがいる。**開いている途中の接続は実接続として報告する** — DB はもうその接続を数えているかもしれず、開き終わる前に枠が空いたと報告すると、その枠が他のテナントへ払い出される。coordinator 側も周期で調停するループ（`InProcessCoordinator::run`）を持つ。事前条件で拒まれた調停は ERROR で記録して次の周期に回す。ノードの起動（`pgsteward` バイナリ）への組み込みと、テナントごとのプールをいつ作っていつ捨てるかは次の CL。**プールの枠数は払い出しで動かす** — `Pool::converge` が上限を差し替え、増えた分は待機しているクライアントに枠を渡し、減った分はアイドルな接続から閉じる（design-doc 8.3 の収束）。**実行中のトランザクションは中断しない** — 使用中の接続は超過していても取り上げず、返ってきた時点で閉じる対象にする。**閉じ終えるまでその枠は空かない** — 閉じ待ちの接続も枠の勘定に入れるので、置き換えが先に開いて実接続が払い出しを超えることがない（M1-08 と同じ理由）。実接続の数え方に閉じ待ちを含めたのもこのためで、DB はまだその接続を数えている。**閉じるのは次の収束にまとめる** — 返却は `Assigned` の drop（同期）で起きるが閉じるのは非同期なので、超過して返ってきた接続は閉じ待ちに積み、次の `converge` が閉じる。制御ループが周期で呼ぶので、遅れは 1 周期に収まる | `pgsteward-core::pool`（`Pool::converge`、`PoolStats::closing`）、テスト `pool`（枠を上げて待機者に渡す、下げてアイドルを閉じる、使用中は中断しない、超過して返った接続をプールに戻さず閉じる、枠 0 では新しいクライアントに枠がない、閉じ終えてから置き換えが開く、同じ払い出しでは何も動かない）、シミュレーション `pool`（縮小が DB 側に届いてから次のクライアントが来ることを `CapMonitor` で確認）、`pgsteward-core::allocation`（`AllocationTable`、`Entry`、`Desired`、`Holder`、`InstanceId`、`ProxyId`、`PreconditionError`）、テスト `allocation`（予算に収まるエントリ、超えるエントリの拒否、拒んだエントリが何も動かさないこと、インスタンスごとの独立、エントリが名指したインスタンスの入れ替え、0 本は載らないこと、予算の縮小は払い出しと同じエントリで来ること、proxy が自分の払い出しだけを読むこと、書かれていないインスタンス、同じエントリの再適用。プロパティテストで「どのインスタンスも予算を超えて持たない」をエントリの列に対して確かめる）、`pgsteward-core::grant`（`GrantChannel`、`InProcessCoordinator`、`GrantSet`、`Report`、`Usage`、`TenantPolicy`）、テスト `grant`（需要どおりの払い出し、調停までは払い出さない、需要 0 は最低保証があっても 0 本、方針のないテナント、総予算のないインスタンス、競合するテナントの分け合い、変化のない調停では世代が進まない、変化した払い出しは新しい世代で配られる、購読者に届く、回収した枠は閉じ終えた報告まで他へ渡さない、縮小より前の世代の報告では解放しない、未報告の払い出しは全量で数える、総予算の増加は即時、縮小は払い出しを連れて下がる、縮小後の超過が閉じるまで増やす側を待たせる。プロパティテストで、払い出しに従うだけの proxy のモデルに対して、要求・調停・開く・閉じる・報告を任意の順で並べても実接続の合計が総予算を超えないことを確かめる）、`pgsteward-core::convergence`（`ProxyPools`）、`pgsteward-core::pool`（`PoolStats::demand`、`PoolStats::occupied`）、`pgsteward-core::grant`（`InProcessCoordinator::run`）、テスト `convergence`（需要と実接続の数え方、払い出しのないプールは開かずに待機を報告する、収束で待機中のクライアントに枠が渡る、払い出しに載っていないプールは 0 本まで閉じる、開いている途中の接続を実接続に数える、制御ループが固定のプールサイズなしにクライアントを捌く、テナント間で枠が動いても総予算を超えない）、シミュレーション `grant`（3 テナント×4 クライアントが総予算 2 を奪い合い、全員が捌かれ、`CapMonitor` で超過が一度もないことをシード 20 本で確認） |
| M1-11 以降 | 未着手 | |

## 用語

design-doc 9 章の用語集に揃える。ここには実装で頻出するものだけを再掲する。

| 用語 | 意味 |
|---|---|
| クライアント接続 | アプリケーションから proxy への TCP 接続 1 本 |
| DB 接続 | proxy から PostgreSQL への TCP 接続 1 本。契約では「サーバー接続」と呼んでいるもの |
| 割り当て | ある DB 接続が、あるクライアント接続に貸し出されている状態。トランザクションモードではトランザクション単位で続く |
| インスタンス | 1 つの PostgreSQL サーバー（`host:port`）。総予算を持つ単位であり、配分の計算が閉じる単位 |
| テナント | `user × database` の組。配分と隔離の単位。接続してよいインスタンスの集合を持つ |
| 接続枠 | DB 接続を 1 本張ってよいという権利。**払い出しの単位**であり、クエリでもトランザクションでもない |
| 割当表（あるべき状態） | 「インスタンス × テナント × proxy → 接続枠の本数」の表。coordinator が計算し、Raft で複製する正典 |
| 総予算 | 1 つのインスタンスに対して本システムが張ってよい接続数の上限。**設定に書かず、システムが `max_connections` と他者の接続の観測から導出する** |
| 需要 | proxy があるテナントについて必要としている接続数。待機キュー長と使用中の接続数から算出し、定期の報告に載せる |
| 要求 | proxy が coordinator に接続枠を求めること。需要の変化時にだけ発生する |
| 払い出し | coordinator が proxy に接続枠を与えること。期限があり、更新できなければ失効する |
| 回収 | coordinator が proxy に接続枠を手放すよう指示すること。proxy はアイドルな接続から閉じ、実行中のトランザクションは中断しない |
| 調停 | 未払い出しの余剰が尽きているときに、あるべき状態を計算し直し、回収と払い出しを組み合わせて要求を満たすこと。他の proxy から能動的に回収する部分をプリエンプションと呼ぶ |
| 収束 | proxy が実接続を払い出しに一致させること。増えた分は張り、減った分は閉じる |
| 自己フェンス | 払い出しを更新できなくなった proxy が、指示を待たずに自ら全接続を閉じること |
| proxy | ワイヤプロトコルを話す実体。クライアント接続を受け、DB 接続を保持する。**配分の判断を持たない** |
| coordinator | あるべき状態を計算し、Raft で複製し、proxy に配る役割。クライアント接続も DB 接続も持たない |

## 1. 全体構成

Cargo workspace を 5 つの crate に分け、M1 では上 4 つを作る。**パッケージ名にはすべて `pgsteward-` を付ける。** `core` は Rust の組み込み crate `core` と衝突するので、パッケージ名は `pgsteward-core`（ディレクトリは `crates/core`）とする。他も同様に `pgsteward-protocol`、`pgsteward-sched`、`pgsteward-node`、`pgsteward-coordinator` である。以下の表と本文では短く `protocol` / `sched` / `core` / `node` / `coordinator` と呼ぶ。

| crate | 責務 | 依存 | M |
|---|---|---|---|
| `protocol` | ワイヤプロトコル v3 のフレーミング、メッセージ型、ReadyForQuery を監視する状態機械。入出力を持たない純粋な層 | `bytes`, `postgres-protocol` | 1 |
| `sched` | **配分アルゴリズム。** 総予算と需要と現在の払い出し量から、あるべき状態（目標の払い出し量）を出す純粋関数のトレイトと、既定実装の重み付き max-min fair。入出力も時間も持たない | なし | 1 |
| `core` | クライアントセッション、DB 接続、プール、待機キュー、リセット、認証、キャンセル、**割当表と配分器と制御ループを 1 プロセス内に持つ縮退形の coordinator（自分自身に払い出す）**。読み書きの抽象に加えて**時計とタスク生成も抽象化**（`rt` モジュール） | protocol, sched, `tokio`, `tokio-rustls`, `scram-rs` | 1 |
| `node` | バイナリ。ノードローカル設定とクラスタ設定の初期値の読み込み、**役割の切り替え**、リスナー、管理コンソール、メトリクス、シグナル処理 | core, `prometheus-client`, `clap` | 1 |
| `coordinator` | **Raft 割当表、要求の受付と払い出し、需要報告の集約、調停と回収（プリエンプション）、失効、自己フェンスの相手方。** coordinator 役割の実体 | core, sched, `openraft` | 2 |

### 1.1 M1 と M2 の境界

この計画の要は、**M1 で作る proxy 側が「払い出しがどこから来たか」を知らない**ことにある。design-doc 8.2 のとおり proxy はあるべき状態を持たず、「今払い出されている枠」を受け取り、「実接続と需要」を報告するだけである。proxy 側のプールが見るのは、その 2 方向のチャネルだけにする。

```
                  core::Pool（proxy 側）
                        │
              trait GrantChannel
        subscribe_grants() / report(demand, actual)
                   ╱            ╲
    InProcessCoordinator（M1）   ClusterCoordinator（M2）
   割当表・配分器・制御ループを   同じ割当表と制御ループを
   同一プロセス内に持ち、        coordinator crate に置き、
   自分自身に払い出す。          Raft で複製して通信で配る。
   要求も払い出しも関数呼び出し   proxy 側は無変更
```

**M1 は新しい制御モデルの縮退形である**（design-doc 13.1）。1 プロセスの中に割当表と配分器と制御ループを持ち、自分自身に払い出す。要求も払い出しも回収も関数呼び出しになるだけで、**design-doc 8.2 の役割の境界はこの時点で引いておく**。境界が引けていれば、M2 で加わるのは通信と合意だけになる。逆に proxy 側のプールに「枠を増やす」「枠を手放す」の判断を持たせてしまうと、分散化のときに書き直しになる。

**配分アルゴリズムそのものは両方で共有される。** M1 は 1 プロセス内の需要だけを入力に回し、M2 は全 proxy の需要を集めて coordinator で回す。入出力の型が同じ（インスタンスごとに、テナント×proxy の最低保証・上限・重み・需要・現在の払い出し量 → 目標の払い出し量）なので、**M1 で書いたプロパティテストが M2 でもそのまま効く**。これが配分器を独立した crate に切り出した理由である（「配分アルゴリズムを純粋関数にする決定」）。

**時間と入出力の抽象化。** 「決定的シミュレーションとモデル検査で正しさを示す決定」により、`std::time::Instant` と `tokio::spawn` と `tokio::net` を直接呼ばない。基盤は**自前の `rt` トレイト**である — `Clock`（現在時刻と `sleep`）、`Spawner`（タスク生成）、`Net`（listen / connect / accept）の 3 つを `core::rt` に閉じ込め、**本番では tokio 実装**を、**`tests/sim` では turmoil を裏に使う実装**を差す。madsim のように tokio を丸ごと差し替える方式は採らない。呼び出し側は `rt` 越しにしか時計もタスクもネットワークも触れない形にして、素朴に書けなくなる範囲を狭める。**これは M1-01 で入れる。** 後から入れることは全入出力の書き換えを意味する。

**設定の 2 層。** design-doc 14.1 のとおり、設定はノードローカル設定（各ノードの TOML。役割、リスナー、coordinator の入口、受け入れ上限、TLS、DB の認証情報）とクラスタ設定（インスタンスの一覧とテナントの `min` / `max` / `weight`。正典は割当表で、TOML は coordinator が空の状態から初期化するときの初期値としてだけ読む）に分かれる。**総予算はどちらにも書かない。** M1 では 1 プロセスが両方を読み、縮退形の coordinator が初期化する。

**ライブラリの使い分け。** DB 側の認証とバックエンドメッセージの解析は `postgres-protocol`。クライアント側の SCRAM サーバー実装は `scram-rs` を評価し、合わなければ RFC 5802 / 7677 に沿って自前で書く。フレーミングは数十行なので自前。`pgwire` は DB サーバーを実装するための枠組みでプロキシには重いので使わない。Raft は `openraft` を第一候補とし、M2-02 で `raft-rs` と比較する。

```
workspace/
├── Cargo.toml
├── crates/
│   ├── protocol/     framing, messages, ready_for_query FSM
│   ├── sched/        trait Allocator, weighted max-min fair
│   ├── core/         session, server_conn, pool, queue, reset, auth,
│   │                 cancel, grant（GrantChannel, InProcessCoordinator:
│   │                 割当表・配分器・制御ループの縮退形）,
│   │                 rt（時計・タスク・ネットワークの抽象。tokio 実装）
│   ├── node/         main.rs, config（ノードローカル / クラスタ初期値）, role, admin, metrics
│   └── coordinator/  （マイルストーン 2）raft, ledger, grant, demand, arbitrate, fence
├── tests/
│   ├── harness/      偽 PostgreSQL、pg_stat_activity 監視、接続上限の共通アサーション
│   ├── integration/  testcontainers で実 PostgreSQL を起動
│   ├── sim/          決定的シミュレーション（rt の turmoil 実装）
│   └── chaos/        (M2) docker compose + toxiproxy（補助）
├── model/            (M2) TLA+ または stateright のモデル
└── bench/            pgbench 比較スクリプト
```

## 2. テスト戦略

「テストを書く → 失敗を確認 → コミット → 実装 → 通過」の順。テスト名には契約の識別子を含める。

| 階層 | 場所 | 内容 |
|---|---|---|
| 単体 | crate 内 | フレーミング、状態機械、プールの状態遷移。偽の入出力で DB なしに走る |
| プロパティ | `sched` / proptest | 配分器が満たすべき 5 つの性質（総和・最低保証・上限・決定性・単調性）をランダム入力で確認。**トレイトの契約なので実装が増えても同じテストが効く** |
| 統合 | `tests/integration` | Docker 上の実 PostgreSQL 16。単一ノードの契約の大半 |
| シミュレーション | `tests/sim` | 時計・スケジューリング・ネットワークを疑似乱数で制御し、シード値で再現。**並行性と分断はここで潰す** |
| 適合・ベンチ | `bench/`, `tests/conformance` | pgbench、asyncpg 風の extended query 連打、COPY |

**接続上限の検証。** 統合テストとカオステストは実行中ずっと `pg_stat_activity` をポーリングし、`application_name` で本システム由来の接続を数え、**インスタンスごとの総予算（導出値）**を一度でも超えたら失敗にする。シミュレーションでは実 DB がないので、代わりに**偽 PostgreSQL が受け入れた接続数**を同じ判定にかける。両方を `tests/harness` の共通のアサーションとして M1-01 で作る。あわせて design-doc 8.4 の性質 2「proxy の実接続数 ≤ その proxy への払い出し」も同じハーネスで常時検査する — 上限の超過より先に、proxy 単体で検知できるからである。

**決定的シミュレーションの使い方。** CI では固定シード集合（回帰）とランダムシード（探索）の両方を回す。失敗したシードは回帰集合に追加する。**失敗報告は必ずシード値を伴う**ので再現に手間がかからない。docker compose のカオステストは、シミュレーションが実環境と乖離していないかの確認用として夜間に回す。

**状態隔離の検証。** 「汚した DB 接続を返し、次のクライアントから見えないこと」を同じ形で確かめる。`SET`、`PREPARE`、`CREATE TEMP TABLE`、`pg_advisory_lock` の 4 パターンを 1 つのパラメタライズドテストにする。

**CI。** `cargo fmt --check` → `clippy -D warnings` → `cargo deny` → `cargo audit` → 単体 → プロパティ → シミュレーション → 統合。シミュレーションは DB が要らないので統合より前に置き、速く失敗させる。

## 3. マイルストーン 1: 単体の予算スケジューラ

**完了条件**: 1 台で単一ノードの契約と予算スケジューラの契約が通り、pgbench で PgBouncer と同等以上のスループットが出る。加えて**テナント 30,000 件を総予算 100 で捌け、3 テナントが競合しても飢餓が起きない**。

M1-06 で初めて psql が繋がり、M1-08 でプーラーになり、**M1-10 で既存プーラーとの差分が出る**。

| # | 内容 | 満たす契約 |
|---|---|---|
| **M1-01** | **足場、時間と入出力の抽象化、テストハーネス。** workspace、CI、ノードローカル設定とクラスタ設定の TOML スキーマ（総予算は項目に持たない）。`rt` の `Clock` / `Spawner` / `Net` トレイトと、tokio 実装 / turmoil 実装の 2 つ。偽 PostgreSQL と `pg_stat_activity` 監視、両者に共通の接続上限アサーション。抽象は `rt` モジュールに閉じ込め、呼び出し側は `rt` 越しにしか時計・タスク・ネットワークを触れない形にして、素朴に書けなくなる範囲を狭める。**このステップを削ると「決定的シミュレーションとモデル検査で正しさを示す決定」が成立しない** | 接続上限の計測基盤、起動時の設定検証 |
| M1-02 | フレーミングと ReadyForQuery 状態機械。入出力を持たない純粋関数 | simple query とトランザクションモードの基盤、トランザクション不可分 |
| M1-03 | スタートアップの判別。SSLRequest / GSSENCRequest / CancelRequest / StartupMessage を見分け、テナントに解決。テナントの識別子は StartupMessage の `user` と `database`（省略時は `user` と同じ、PostgreSQL の既定に従う）から作る。**この識別子とクラスタ設定の `[tenant."…"]` の対応づけは M1-10 で行う** — 設定の鍵の書式（`user` だけか `user@database` か）は design-doc 14.1 で未定なので、そこで決める。GSSENCRequest は SSLRequest と同じく判別だけ行い、応答の方針（`N` を返す）は M1-05 で入れる | スタートアップ、テナント解決と認証 |
| M1-04 | DB 接続の確立と総予算の導出。SCRAM / MD5、ParameterStatus と BackendKeyData の保持、`application_name` に識別子。**`SHOW max_connections` / `superuser_reserved_connections` / `reserved_connections` を読み、`pg_stat_activity` で他者の接続を観測して、インスタンスごとの総予算を導出する**。`tcp_keepalives_idle` と `tcp_user_timeout` の検証もここで読む | 起動時の設定検証、総予算の導出、パラメータ通知の DB 側 |
| M1-05 | クライアント認証。**この時点で DB 接続も接続枠も消費しない** | 認証、接続受け入れ時に資源を消費しない |
| **M1-06** | **セッションモードの透過プロキシ。** クライアント接続と DB 接続を 1 対 1 に結び、双方向にバイト列を流す。**ここで初めて psql で SELECT が動く** | simple query、エラーの透過、セッションモード |
| M1-07 | プールと待機キュー。遅延生成、FIFO の待機キュー、待機タイムアウト。**アイドルタイムアウトは持たない** — 使われていない接続を閉じる判断は枠を手放す判断と同じもので、coordinator が下す（design-doc 8.4）。**上限はまだ固定値で、M1-10 で割当表の払い出しから取るように差し替える** | 待機キュー、遅延接続、ノードの受け入れ上限、排他割り当て |
| **M1-08** | **トランザクションモード。** 最初のメッセージで DB 接続を割り当て、ReadyForQuery がアイドルを示した時点で返す。トランザクション中と失敗したトランザクション中は保持する。**ここでプーラーになる**（ゴール 1） | トランザクションモード、トランザクション不可分、返却と応答の同一性 |
| M1-09 | 配分アルゴリズム。`sched` crate。純粋関数なので DB も時計も要らない。入力に**現在の払い出し量**を含め、ヒステリシスをアルゴリズム側の責務にする。整数丸めの誤差は繰り越す。総予算が最低保証の合計を下回ったときは最低保証を重みに比例して縮小し、その旨を出力に含める | 総和、最低保証の確保、上限、決定性、単調性 |
| **M1-10** | **割当表・配分器・制御ループの縮退形（1 プロセス内 coordinator）。** `GrantChannel` と `InProcessCoordinator`。proxy 側は需要と実接続を報告し、プールの上限を割当表の払い出しに差し替える。払い出しの総和 ≤ 総予算を割当表の事前条件として検査する。**需要が消えたテナントのプールオブジェクトは coordinator の回収指示で破棄**され、最低保証 0 のテナントは需要がない間プールオブジェクトを持たない。**ここで差分が出る**（ゴール 3, 4） | 割当表の健全性、最低保証、払い出しと実接続の対応、ゼロ保証テナント |
| M1-11 | extended query の Sync 境界と COPY。Flush への応答中に割り当てを解除しない。300 行を超えるなら 2 つに割る | extended query、COPY |
| M1-12 | 状態リセット。`DISCARD ALL` の成功を確認してからプールに戻す。**DB 接続を捨てても枠は保持したまま張り直す** | 状態リセット、セッション変数・一時テーブル・アドバイザリロックの扱い、状態隔離 |
| M1-13 | クライアント切断の回収。待機中ならキューから除去し、需要の報告から外す | クライアント切断、切断時の回収 |
| M1-14 | キャンセル。クライアントに返す鍵は自前で生成し、`pid` の上位ビットに proxy の識別子を埋めて M2 の転送に備える | キャンセル、パラメータ通知、キャンセル対象の特定 |
| M1-15 | TLS。rustls で終端、DB 側は `sslmode` 相当の設定 | TLS |
| M1-16 | プリペアドステートメント非対応の明示。記録にない名前への Bind を SQLSTATE 26000 で拒否し、**エラー本文に 4 ドライバの無効化方法を含める** | プリペアドステートメント、LISTEN / NOTIFY |
| M1-17 | 管理コンソール、メトリクス、グレースフル停止。`SHOW POOLS` / `SHOW BUDGET`（**あるべき状態と実接続を並べて返す**）/ `SHOW INSTANCES`（導出した総予算とその内訳）、Prometheus と OpenTelemetry。停止時は縮退形の coordinator に払い出しを返してから閉じる | 可観測性、グレースフル停止 |
| **M1-18** | **適合とベンチマーク。M1 の成果をここで公開する** | 単体スループット、接続チャーン |

## 4. マイルストーン 2: 分散層

**完了条件**: coordinator 3 台 + proxy 3 台で、どの proxy に繋いでも同じサービスを受けられ、`kill -9`・分断・時計ずれを注入した決定的シミュレーションで DB 接続数が一度も総予算を超えず、再配分の収束時間・復旧時間・接続チャーンの目標値を実測で確定させる。

配置方式の比較は**「分割配置と予算リースの決定」**から**「配分を誰が決めるか」（design-doc 20.2）**に移した。本書は決定済みの方式を実装する手順だけを扱う。

### 4.1 払い出しの安全性

Raft は「誰が何本持つべきか」の割当表であり、**それだけでは足りない** — Raft のクォーラムと対象 PostgreSQL の故障ドメインは一致しないので、自己フェンスと DB 側の保険を重ねる（「自己フェンスの決定」）。design-doc 8.4 の 2 つのローカルな性質（払い出しの総和 ≤ 総予算、実接続 ≤ 払い出し）をつなぐ過渡状態を、次の規則で塞ぐ。

1. **要求と払い出し。** proxy は需要が変化したときだけ coordinator に要求する。枯渇したときは定期の報告を待たずに即時要求を上げる。coordinator は調停周期で全 proxy の需要から配分器であるべき状態を計算し、現在の割当表との差分を 1 つのログエントリにまとめて Raft にコミットしてから、期限つきで払い出す。値には proxy の識別子と Raft のログインデックス（フェンシングトークン）を入れる。**割当表の状態機械は「インスタンスごとに払い出しの総和 ≤ 総予算」を事前条件として検査し、破るエントリはコミットしない**
2. **更新と需要報告。** proxy は期限の 3 分の 1 の間隔で払い出しを更新し、あわせて需要と実接続数を報告する。経過時間は**単調時計**で測る。**proxy は自分の判断で枠を増やさず、減らしもしない** — 需要が消えたことを報告するだけで、割当表から外すのは coordinator である
3. **自己フェンス。** 経過時間が「期限 − 余裕」を超えたら、新規の割り当てをやめ、割り当て中の DB 接続にキャンセルを送り、すべて閉じる。**余裕**は「DB 接続を全部閉じるのにかかる時間 + 時計ずれの許容」以上に取る。proxy の単独判断で動くのはここだけで、向きは減らす方向に限られる
4. **失効と再配分。** coordinator が失効と見なすのは最後の更新から期限が過ぎたとき。規則 3 により**旧保有者の自己フェンスは常にそれより前に終わっている**。失効した枠は割当表から外し、次の調停で要求のある proxy へ配り直す
5. **調停の順序。** 接続枠を proxy A から proxy B へ動かすとき、coordinator は A に回収を指示し、**A の回収完了報告が Raft にコミットされるより前に、その分を B へ払い出してはならない**。この順序だけが移動中の接続上限を守っている。A はアイドルな接続から閉じ、実行中のトランザクションは終わってから閉じる
6. **DB 側の保険。** クラッシュして自己フェンスできない場合に備え、`tcp_keepalives_idle` と `tcp_user_timeout` を払い出しの期限より短く設定し、**起動時に `SHOW` で検証して満たさなければ起動を拒否する**（rev.1 では警告だったが拒否に格上げ）

**証明の外で仮定していること**は design-doc 8.4 の 3 つ（単調時計が単調であること、余裕が十分に大きいこと、DB 側のタイムアウトが実際に効くこと）で、**分断から復帰した proxy の再同期**は未解決のまま残っている（design-doc 19 章）。この 4 つを M2-09 のモデル検査で最初に当てる。

**受け入れる代償**: 分断された proxy は「自分が正しいかもしれない」場合でもサービスを止める。そのテナントは最大「期限 + 再取得時間」の間、その proxy 上で利用できない。加えて coordinator 群が落ちると需要の増加に一切応えられない（既存の払い出しは期限まで有効）。

### 4.2 ステップ

| # | 内容 | 満たす契約 |
|---|---|---|
| M2-01 | クラスタのシミュレーションハーネス。N ノードを 1 プロセス内で動かし、kill / 分断 / 遅延 / 時計ずれをシードから決める。M1-01 の `rt` の turmoil 実装をそのまま使う。docker compose 構成は補助 | 接続上限の分散検証、復旧時間の計測 |
| M2-02 | Raft 割当表と coordinator 役割。`openraft` を評価（`raft-rs` と比較）。割当表の状態機械の事前条件（4.1 の規則 1）をここで入れる。クラスタ設定（インスタンスとテナントの配分）も割当表に載せ、書き込み時に `Σ min(T) ≤ 総予算(I)` を検証する。ブートストラップは StatefulSet の序数を使う方式を試す | 割当表の健全性、ノード参加、クラスタ設定の正典 |
| M2-03 | 要求・需要報告・払い出し。`ClusterCoordinator`（proxy 側の `GrantChannel` の M2 実装）と coordinator 側の受付。定期の更新と需要報告 + 枯渇時の即時要求。proxy は起動時に設定の入口 1 つに繋ぎ、リーダーからメンバーシップとクラスタ設定を受け取って保存する。**M1-09 の配分器と M1-10 の制御ループをそのまま使う** — 入力が 1 ノード分から全ノード分に変わるだけ。即時要求で枯渇時のラウンドトリップを 1 回に抑えるが、それでも遅延が大きければ調停周期の短縮を検討する（proxy に予備の枠を持たせる案は、proxy に判断を戻すことになるので採らない） | 払い出しと実接続の対応、再配分の収束時間 |
| **M2-04** | **自己フェンス。** 4.1 の規則 2・3・6。起動時に DB 側設定を検証し、満たさなければ**起動を拒否** | 自己フェンス、DB 側タイムアウトの検証 |
| **M2-05** | **調停と回収（プリエンプション）。** 4.1 の規則 5。回収指示 → アイドルな接続から閉じる → 完了報告 → コミット → 払い出し。実行中のトランザクションは中断しない。総予算の縮小（他者の接続の増加やマージンの変更）も同じ経路で収束させる | 調停、移動の安全性、回収の完了 |
| **M2-06** | **フェイルオーバー。** 4.1 の規則 4。払い出しの失効と再配分。coordinator 全滅時の挙動（既存の払い出しは期限まで有効、需要の増加には応えない）も実測して文書化 | 障害時の再配分、接続上限、復旧時間 |
| M2-07 | キャンセルの転送。**proxy 間に必要なのはこの制御メッセージだけで、データの中継路は持たない** | キャンセル、キャンセル対象の特定 |
| M2-08 | 計画停止とローリング更新。SIGTERM で払い出しを自分で coordinator に返すので、期限を待たずに枠が残った proxy へ回る。クラスタ設定の変更は管理コンソールから coordinator へ 1 回書き込み、再起動なしで配られる | ノード離脱、計画停止、無停止の運用 |
| M2-09 | モデル検査とのつき合わせ。4.1 の規則と design-doc 8.4 の仮定を対象にし、反例をシミュレーションのシードに変換して回帰集合へ | 自己フェンス、移動の安全性 |
| **M2-10** | **カオステストと目標値の実測。** 暫定値を実測に置き換える。**シードと実測値を公開する** | 再配分の収束時間、復旧時間、接続チャーン |
